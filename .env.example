# Build-time only. When set, `npm run build:catalog` assembles the preset catalog
# from live Mobility Database GTFS feeds instead of the bundled fallback.
# Get your refresh token from your account at https://mobilitydatabase.org
# (Account Details). Put the real value in .env.local — never commit it.
MOBILITY_DATABASE_REFRESH_TOKEN=
